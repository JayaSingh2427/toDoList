<%- include('header') -%>
<body>
    <!-- <% if(listTitle === "Sunday"){ %>
        <h1 style="color:purple"><%= listTitle %>ToDoList</h1>
    <% } else {  %>
        <h1 style="color:yellow"><%= listTitle %>ToDoList</h1>
    <% }  %> -->
<div class="box" id="heading">
<h1><%= listTitle %></h1>
</div>   
       <div class="box">
        <% for(var i=0; i<newListItem.length ; i++){ %>

            <div class="item">
                <input type="checkbox" >
           <p> <%= newListItem[i] %></p>
        </div>

        <% } %>

        <form class="item" action="/" method="post">
            <input type="text" name="myItem" placeholder="New item" autocomplete="off">
    
            <button type="submit" name="button" value=<%= listTitle %>>+</button>
        </form>
    <%- include('footer') -%>